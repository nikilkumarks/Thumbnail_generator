services:
  - type: web
    env: node
    name: youtube-thumbnail-ai-backend
    plan: free
    buildCommand: cd frontend && npm ci && npm run build && cd ../backend && npm ci
    startCommand: cd backend && npm start
    autoDeploy: true
    healthCheckPath: /api/health
